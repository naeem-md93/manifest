---
globs: "**/*.py"
description: Ensure that all backend endpoints are built using FastAPI to
  support asynchronous operations, automatic documentation (Swagger UI), and
  integration with Python-based services.
alwaysApply: true
---

Always use FastAPI for building the backend API for the life and goal management application